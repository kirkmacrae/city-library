<p id="notice"><%= notice %></p>

<h1>Book Listing</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Genre</th>
      <th>Subgenre</th>
      <th>Pages</th>
      <th>Publisher</th>
      <th>CopiesAvailable</th>      
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.genre %></td>
        <td><%= book.subgenre %></td>
        <td><%= book.pages %></td>
        <td><%= book.publisher %></td>
        <td><%= book.copies %></td>
        <% if current_user %>        
        <td><%= link_to 'Borrow', my_books_return_path %></td>
        <% end %>        
      </tr>
    <% end %>
  </tbody>
</table>

<br>
